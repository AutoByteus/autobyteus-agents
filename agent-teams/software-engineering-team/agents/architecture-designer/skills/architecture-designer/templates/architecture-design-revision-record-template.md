# Architecture Design Revision Record

Create `architecture-design-revision-record.md` before the initial architecture-review handoff. Add `AD-REV-001` as the concise initial architecture baseline, then append one entry for each later completed architecture-design round; do not create round-specific copies.

The approved requirements package and latest design spec remain authoritative. Record only the initial design baseline or later architecture rework delta and rationale needed to navigate those artifacts. Do not reinterpret or revise approved requirements here.

## Revision Index

| Revision ID | Triggering Role / Report / Round | Finding IDs | Classification | Result |
| --- | --- | --- | --- | --- |
| AD-REV-001 |  |  | `Initial Architecture Baseline` / `Design Impact` / `Unclear` |  |

## Revision Entries

### AD-REV-001 — `<concise revision title>`

- Triggering role, report path, and round:
- Triggering finding IDs:
- Prior authoritative design result: `N/A` / result
- Current authoritative design result:
- Why this baseline or revision is recorded:
- Approved behavior or requirement IDs affected:
- Design-spec sections updated:
- Architecture supplements updated, added, or removed:
- Downstream and architecture-review impact:
- Next recipient or routing:
- Remaining gaps or risks:

For `AD-REV-001`, record the initial architecture baseline and use `N/A` for the prior result and triggering findings. Keep prior entries unchanged except to correct a factual error.
