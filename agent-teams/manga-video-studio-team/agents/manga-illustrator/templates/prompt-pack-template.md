# Prompt Pack

## Purpose

- This file is the durable prompt source for the chapter visual run.
- Keep prompts reusable enough that later chapters can inherit locked character and location language instead of starting from scratch.

## Run Metadata

- Chapter id:
- Render-unit contract:
- Inherited locked series aspect ratio and orientation copied from `series-bible.md`:
- Preferred image route:
- Source visual canon refs, including `series-bible.md` and `visual-style-guide.md`:

## Character Lock Blocks

### Character: [character-id]

- Core lock block:
- Required visible traits:
- Forbidden drift:
- Reusable reference asset paths:

## Location Blocks

### Location: [location-id]

- Core environment block:
- Required recurring props:
- Atmosphere and lighting notes:
- Reusable reference asset paths:

## Render-Unit Prompt Coverage

For video delivery, this section should cover video frames. Page or panel prompt entries belong here only when `page-composed` or `panel-first` was explicitly selected.

### Render Unit: [storyboard-id]

- Prompt objective:
- Render-unit type:
- Inherited locked series aspect ratio and orientation copied from `series-bible.md`:
- Requested generation size or dimensions when supported:
- Exact final prompt to send to the image tool, including the locked ratio/orientation phrase for `video-frame`:
- Locked ratio phrase present in final prompt:
- Visible text spec:
- Negative prompt:
- Input reference assets:
- Lettering expectation:
- Video-frame conformance notes:

## Global Negative Prompt Block

- Drift risks to suppress:
- For `video-frame` assets: no manga page, no panel grid, no page border, no white paper margin, no collage, no contact sheet, no reference sheet, no watermark.

## Notes For Future Chapters

- Prompt fragments worth reusing:
- Prompt fragments that caused drift:
