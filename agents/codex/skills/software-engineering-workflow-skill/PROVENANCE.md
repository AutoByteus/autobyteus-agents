# Bundled skill provenance

This is the portable, run-snapshotted Codex workflow skill bundled with the agent package.
Copied from `autobyteus-skills/software-engineering-workflow-skill` at source commit
`6db634c118ccc6193428fe6bf7422d22357e42d3` on 2026-09-26.

The `autobyteus-agents` package maintainer owns deliberate updates: compare with the
upstream skill, copy its complete referenced tree, update this pin, and validate the
package. A configured skill that is truly missing is warned and omitted at AGY run
preparation; this normal package intentionally contains the skill.
